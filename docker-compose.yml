services:
  backend:
    build:
      context: ./apps/backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      SERVER_PORT: 8080
      SQLITE_FILE: /data/linkedin-scraper.sqlite
    volumes:
      - ./data:/data
